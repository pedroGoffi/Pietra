use std{std, varargs}

main :: (){        
    printf("..hello, world '%s': %i\n", "My Printf parameter", 69420, 0)

    buffer := new [256] i8
    sprintf(&buffer, "Hello %s.", ", world", 0)
    printf("Readed the buffer using printf, got = '%s'\n", buffer, 0)

    return 2
    
}