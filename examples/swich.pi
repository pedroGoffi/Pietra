use std{std}


main :: (){    
    y: cstr = "bar"

    switch(y){
        case 'bar': {            
            std.puts("[TEST_OK]: y == 'bar'\n")
        }

        default:{
            std.puts("[TEST_FAILED] y != 'bar'\n")
        }
    }
}