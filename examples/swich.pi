
x :: "foo"



main :: (){
    y: cstr = "bar"
    switch(y){
        case "bar": {
            dump(x)
            dump(y)
            std.puts("ok it's x\n")
            std.puts(x)
            std.puts("|")
            std.puts(y)
            std.puts("\n")
        }

        default:{
            std.puts("not x\n")
        }
    }
}