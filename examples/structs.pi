use std{std}

String :: {
    data: cstr
    size: i64
}

main :: (){
    str: String;
    str.data = "hello, world\n"
    str.size = std.strlen(str.data);

    std.puts("str.data = ")
        std.puts(str.data)
    std.puts("str.size = ")
        dump(str.size)
}