use std{std}

Str :: {
    data: cstr
    size: i64
}

impl Str {
    __eq__ :: (self: *Self, data: cstr){
        self.data = data
        self.size = strlen(self.data)
    }
        
}

main :: () {
    str: Str = "hello, world\n"



    std.puts(str.data)
}