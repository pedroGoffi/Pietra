use std{std}

main :: (argc: i64, argv: **i8): i64 {    

    while 0 < argc {        
        std.puts("Got argv = ")
        std.puts(*argv)
        std.puts("\n")
        
        argv = argv + 8;
        argc = argc - 1;
    }
}