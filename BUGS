Corruption on the name on big enumerations


rsi = *VAR
rdx = LEN
rcx = 1

rax = syscall_write (1)
rbx = stdout
rcx = ptr
rdx = len

   movl $1, %ebx   ; write to stdout
    movl $msg, %ecx ; use string "Hello World"
    movl $12, %edx  ; write 12 characters
    int $0x80   