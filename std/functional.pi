#package "pietra.functional"
use std{std}
// TODO: impl more procedures in functional module

impl std {
    value_or :: (val: any, or_v: any){
        if val {
            return val;
        }
        
        return or_v;        
    }    
}